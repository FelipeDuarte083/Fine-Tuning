git init

git status

git add NOME DO ARQUIVO A SER ADICIONADO

git commit -m "Título do commit"

git branch -M "mudar nome da branch para"

git config --global user.name "Nome e sobre nome"

git config --global user.email "endereço@mail.com"